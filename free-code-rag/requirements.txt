# Pinned to a verified-working set. Unpinned installs let torch/transformers resolve to
# mismatched versions across machines, which produced the
# "Cannot copy out of meta tensor; no data!" error during indexing.

# Document loaders / parsing
pypdf==6.7.5
pymupdf==1.26.5
python-docx==1.2.0

# Embeddings + vector store
sentence-transformers==5.1.2
faiss-cpu==1.13.0

# Critical transitive deps for model loading — pinned to avoid the meta-tensor bug
torch==2.8.0
transformers==4.57.6
huggingface-hub==0.36.2

# LangChain (chunking / loaders)
langchain-core==0.3.83
langchain-community==0.3.31
langchain-text-splitters==0.3.11

# HTTP server
fastapi==0.128.8
uvicorn[standard]==0.39.0

# Tests
pytest==8.4.2
pytest-cov==7.0.0
